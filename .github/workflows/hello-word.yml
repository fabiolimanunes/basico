name: 'Hello World'

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  ci-cd:
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v3
      - name: Docker-Build
        run: docker build -t teste .
   
